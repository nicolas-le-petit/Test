*Initital

git --version -> check version

*Config

git config user.name = "user_name"//config user name
git config user.email = "user_email"//config email

*Set up

//go to path

right click -> Git Bash Here -> 
Cmd -> git init -> make a repo in local PC

//check
git status -> check commit status
git log -> log list commit


//Push project to github
git add <file> / git add . (all) -> add new state/change in repo
git commit -m <"explain"> -> commit the change
git remote add <name> <url> -> announce a repo <name> 
git push --set-upstream <name> <branch> -> push the repo in local PC to Internet

//fetch project to pc
git clone <url> -> take a repo from Internet via url

//revert local
git log -> lay id commit
git revert <id>

//change on Github
//get new version in github
git pull


process:
git init
git status (check)
git add .
git commit -m "message"
git log (check)
git remote add <name> <url>
git push -u <name> <branch>

sua test choi

test conflict choi
